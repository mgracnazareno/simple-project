# simple-project

Mini coding activity to practice working with a small static UI, Trello, Git, and Live Server.

This project displays a simple message, an illustration, and two buttons to change/reset the message.

---

## 🧩 Activity Description

This project was used for a **40-minute mini coding activity** with the following goals:

- Open the project `simple-project` in VS Code.
- Use **Trello** to track each coding task.
- Use **Live Server** to preview changes in real time.
- Share the screen briefly to show the setup working.

---

## ✅ Tasks Completed

The following tasks are tracked in Trello as individual activities:

- [x] Move message to the bottom (after the sun image)
- [x] Change the static welcome message
- [x] Move the buttons to the top (before the sun image)
- [x] Compress and submit the entire project
- [x] Submit the group project
  - [x] Only one member submits
  - [x] Add the group members inside `README.md`

---

## 🧪 Project Files

Main files used in this activity:

- `index.html` – main page with the static UI:
  - Buttons section (`Change message`, `Reset`)
  - Image section (`picture.svg`)
  - Message:  
    `Hello — Welcome to Mary & Kylene's version of static UI!`
- `css/style.css` – styles for layout and components
- `js/main.js` – JavaScript to handle the message change/reset behavior
- `images/picture.svg` – decorative illustration (the “sun” image)

---

## ▶️ How to Run the Project

1. Open the project in **Visual Studio Code**.
2. Install the **Live Server extension** (if it isn’t already installed).
3. Open `index.html` in the editor.
4. Right-click anywhere on the page → select **Open with Live Server**.
5. The browser will refresh automatically as you make changes.

## 👤 Group Members

**Team Members**
- Mary
- Kylene

